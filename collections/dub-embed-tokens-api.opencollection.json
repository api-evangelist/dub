{
  "opencollection": "1.0.0",
  "info": {
    "name": "Dub Analytics Embed Tokens API",
    "version": "0.0.1"
  },
  "items": [
    {
      "info": {
        "name": "Embed Tokens",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Create a referrals embed token",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://api.dub.co/tokens/embed/referrals",
            "body": {
              "type": "json",
              "data": "{}"
            },
            "auth": {
              "type": "bearer",
              "token": "{{bearerToken}}"
            }
          },
          "docs": "Create a referrals embed token for the given partner/tenant. The endpoint first attempts to locate an existing enrollment using the provided tenantId. If no enrollment is found, it resolves the partner by email and creates a new enrollment as needed. This results in an upsert-style flow that guarantees a valid enrollment and returns a usable embed token."
        }
      ]
    }
  ],
  "bundled": true
}